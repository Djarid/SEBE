# SEBE Daemon Configuration
# Copy this to ../../.env and fill in the values.
# NEVER commit the actual .env file.

# --- GitHub (existing) ---
GITHUB_TOKEN=
GITHUB_USER=Djarid
GITHUB_REPO=SEBE

# --- Proton Bridge ---
# On first run, the container auto-logs-in using these credentials.
# PROTON_USERNAME is your full Proton email address (e.g. user@pm.me).
# PROTON_PASSWORD is your Proton account password (NOT the bridge password).
# After first login, the bridge password appears in the container logs.
# PROTON_TOTP_SECRET is your TOTP base32 secret (optional, only if 2FA enabled).
PROTON_USERNAME=
PROTON_PASSWORD=
# PROTON_TOTP_SECRET=

# --- Email ---
# PROTON_BRIDGE_PASSWORD is the IMAP/SMTP password generated by Proton
# Bridge on first login (appears in container logs). This is NOT the same
# as your Proton account password above.
PROTON_BRIDGE_PASSWORD=
EMAIL_SENDER=

# --- Signal ---
# Your phone number in E.164 format (e.g. +447xxxxxxxxx)
SIGNAL_ACCOUNT=
SIGNAL_OWNER_NUMBER=
# Override if signal-cli REST API is on a different host/port
# SIGNAL_API_URL=http://localhost:8082

# --- Bluesky ---
# App password from Settings > App Passwords (not your login password)
BSKY_HANDLE=jasonhuxley.bsky.social
BSKY_PASSWORD=

# --- LLM ---
# Override if llama-server is on a different host/port
# LLM_BASE_URL=http://localhost:8080/v1
# LLM_API_KEY=sk-local-no-auth
